# Scenario 22 current result-surface verification

Scenario 22 now has fresh preparation-to-result evidence on both current
candidates:

| Profile | Candidate | Runtime-clear probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CB53` / `00f0dec…` | `5812` / `2fddd78…` | `normal/runtime02` |
| Hard | `E15E` / `f3d5e05…` | `6E1D` / `2c64319…` | `hard/runtime01` |

Neither candidate was changed or promoted. Each ignored diagnostic ROM keeps
all eight player deployments, all twelve fixed records, identities, classes,
levels, positions, combat values, and event handlers byte-identical to its
input candidate. The current normal records additionally match the Japanese
source; the hard candidate intentionally retains its own balance data. Opening
Start marks only combat runtime groups 9..19 defeated. It does not touch the
allied Liana in group 8.

The probe changes 202 bytes including its checksum. Replaying the exact normal
diagnostic delta over the current hard candidate has zero hard-mode conflicts;
the only subsequent operation is checksum recalculation.

## Fresh normal and hard flow

Both profiles were entered afresh through the selector and identified Scenario
22. The runtime identity matcher reports 11/12 because Liana's source fixed
class `97` is normalized by the game to playable Cleric class `02`; the other
eleven records match. The diagnostic lineage separately proves an exact 12/12
fixed-record preservation.

Both runs passed preparation, automatic deployment, the turn-1 command
surface, the Korean Start menu, the ordinary End Turn command, every completion
event, all level-up/class-change pages, `전과보고`, and the real `저장` screen.
The retained pre-Start and post-Start GSTs prove Liana's complete runtime record
is identical before and after the wrapper: class `02`, name ID `02`, HP10,
undefeated, at `(14,4)`. All eleven combat groups are defeated with HP0 and an
off-map X coordinate.

All 167 normal aftermath frames were manually reviewed. The 23 hard frames
whose pixels differ from normal were then reviewed side by side; the other 144
are pixel-identical. The bounded differences are animation/capture phase and
hard-profile numeric values. No frame has a broken name, class, commander,
mercenary, portrait, map sprite, or UI glyph.

The sequence retains `보젤`, `라나`, `베른하르트`, `제국군지휘관`,
`리치`, `아이언골렘`, `제시카`, `쉐리`, `레스터`, `헤인`, `엘윈`,
`리아나`, `아론`, and `스코트`. Liana's repeated level-up and class-change
surfaces preserve `클래스체인지 가능` and all visible class and spell names.
The result roster visibly contains the faction heading `아군`, followed by
`엘윈`, `헤인`, `쉐리`, `아론`, `키스`, `레스터`, `제시카`, and
`스코트`, plus `POINT 4480P`. The save menu is intact in both profiles.

Machine-readable evidence is generated by
`tools/verify_scenario22_current_result_surface.py` in
`localization/scenario22_current_result_surface_regression.json`.

## Rejected assertion

The first normal run stopped after the Start menu because its verifier compared
Liana's runtime class against source-only value `97`. The saved GST proves the
wrapper had not changed Liana; the game had normalized her to runtime Cleric
class `02` before Start. The accepted runner no longer relies on that assumption
and compares her complete record before and after Start instead.

These GSTs are isolated verification fixtures. Player progress still migrates
by loading the `.srm` through the in-game menu under the current ROM and then
creating a fresh savestate.
