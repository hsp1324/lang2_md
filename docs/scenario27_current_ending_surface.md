# Scenario 27 current ending-surface verification

Scenario 27 now has fresh, uninterrupted selector-to-`Fin` evidence for both
focused current candidates:

| Profile | Candidate | Adjacent-Bernhardt probe | Accepted run | `Fin` |
| --- | --- | --- | --- | ---: |
| Normal | `CAF3` / `178e704…` | `1661` / `8806c38…` | `normal/runtime08` | 2957 |
| Hard | `E0FE` / `9c6282c…` | `1B5F` / `1a7d3c6…` | `hard/runtime01` | 2960 |

Neither candidate was released or promoted. Each ignored diagnostic ROM is an
exact rebuild from its focused input. It changes only Bernhardt's AT, DF,
position, six mercenary slots, and the Mega Drive checksum: 11 changed bytes
in total. Identity, name, class, level, flags, every other fixed record, player
deployments, and event code remain untouched. The hard-mode record tag is
preserved.

Both accepted runs start at the built-in selector, identify Scenario 27 with
all ten fixed records matching, enter preparation, use automatic deployment,
and reach the ordinary Elwin/Bernhardt target and battle surfaces. The retained
post-battle RAM has Bernhardt at class 78/name 14, coordinates (15,15), and HP
zero. The stock death handler is then allowed to finish normally.

The normal run retains 36 battle frames and 2,957 ending frames; the hard run
retains 36 battle frames and 2,960 ending frames. Every recorded file and frame
sequence is hash-locked. Small index differences are autonomous animation
timing, so equivalent pages are compared by content hash. Both profiles are
pixel-identical on preparation, the first command panel, the Bernhardt target,
and the final `Fin` screen.

Current normal and hard playback exactly reproduce historical reviewed pages
for the opening montage, Scott, Lana, Bozel, Leon, Liana, Elwin, and `Fin`.
The hard Keith, Egbert, Bernhardt, and `Fin` captures were also reviewed
directly. Korean text, portraits, class/status graphics, map sprites, and
backgrounds are intact with no Japanese residue or dynamic-glyph corruption.

The runner confirms only portrait dialogue and white-on-black closing captions.
Credits and cinematics advance without injected confirmations so the stable
`Fin` page is not skipped. The default ending bound is 3,200 captured frames.
An experimental GST continuation was rejected after BlastEm returned to the
Sega/title sequence; accepted evidence therefore always uses a fresh,
uninterrupted selector run.

Machine-readable evidence is generated by
`tools/verify_scenario27_current_ending_surface.py` in
`localization/scenario27_current_ending_surface_regression.json`.
