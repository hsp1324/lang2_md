# 헤인 워록 원본 기반 소서러

헤인 소서러 `09`의 이전 AI 실루엣을 사용하지 않는다. 비-AI 기본
워록 `03`의 ROM 16×16 좌표를 마스터로 사용하고, 현재 소서러 얼굴
마스크를 복원한 뒤 왼쪽 망토만 초록·연두 계열로 바꿨다. 오른쪽
지팡이 수정 한 픽셀을 포함해 16개 행·열을 사용한다.

- ROM 마스터: `master/hein-03-warlock-rom.png`
- 최종 16×16: `logical16/05-09.png`
- 확대본: `previews/05-09.png`
- 검증: `validation-report.json`

이 작업은 에디터 PNG만 바꾸며 ROM에는 적용하지 않는다.
