# 헤인 소드마스터 공통 템플릿 v1

사용자가 편집해 승인한 헤인 소드마스터 `5:1A`의 갑옷·쌍검·실루엣
좌표를 소드마스터 공통 기준으로 사용한다.

헤인 본인은 형태·쌍검·은청색 갑옷을 그대로 두고, 기존 적갈색 망토와
천 명암만 헤인 로드·하이로드와 같은 짙은 초록 `#246D00`, 연두
`#24B624`로 바꾼다. 얼굴·청색 머리와 검날 색은 변경하지 않는다.

엘윈 소드마스터 `1:1A`는 제외하고 현재 디자인을 그대로 보호한다.
소드마스터가 있는 나머지 헤인·키스·아론·제시카에는 현재 얼굴 마스크와
원본 클래스 팔레트를 적용한다. 얼굴 마스크의 투명 좌표에서는 쌍검과
갑옷 픽셀을 우선한다.

아론 소드마스터는 회색 의상 대신 진청 `#2449DB`, 파랑 `#496DFF`,
하늘색 `#6DDBFF` 명암을 사용해 아론 하이로드·하이마스터와 색 계열을
공유한다. 은색 쌍검과 얼굴·머리 색은 바꾸지 않는다.

키스 소드마스터는 사용자가 다시 저장한 66픽셀 머리 마스크로 얼굴·머리를
재복원한다. 기존 금갈색 천·갑옷은 키스 로드·세인트와 같은 진청
`#0024B6`, 파랑 `#496DFF`, 하늘색 `#6DDBFF`로 바꾸고 흰 쌍검은
그대로 유지한다.

- 사용자 승인 기준: `master/hein-1A-swordmaster-user-approved.png`
- 실제 빌드 입력: `logical16/`
- 전체 비교: `all-swordmaster-variants.png`
- 검증: `validation-report.json`
- 적용 전 디자인 보관:
  `archive/shared-swordmaster-before-hein-v1/`
