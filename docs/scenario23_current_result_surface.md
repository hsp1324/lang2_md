# Scenario 23 current result-surface verification

Scenario 23 has fresh preparation-to-result evidence on both current
candidates:

| Profile | Candidate | Runtime-clear probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CB53` / `00f0dec…` | `5812` / `2fddd78…` | `normal/runtime01` |
| Hard | `E15E` / `f3d5e05…` | `6E1D` / `2c64319…` | `hard/runtime01` |

Neither candidate was changed or promoted. Each ignored diagnostic ROM keeps
all nine player deployments, all eleven fixed records, identities, classes,
levels, positions, combat values, and event handlers byte-identical to its
input candidate. The current normal records also match the Japanese source.
Opening Start marks only hostile runtime groups 9..19 defeated and does not
alter player groups 0..8.

The probe changes 202 bytes including its checksum. Replaying the exact normal
diagnostic delta over the current hard candidate has zero hard-mode conflicts;
the only subsequent operation is checksum recalculation.

## Fresh normal and hard flow

Both profiles were entered afresh through the selector and identified Scenario
23 with all eleven fixed enemy records matched. Both runs passed preparation,
automatic deployment, the turn-1 command surface, the Korean Start menu,
ordinary End Turn, every completion event and class-change page, `전과보고`,
and the real `저장` screen. Retained GSTs prove the complete records of all nine
player groups are identical before and after Start. Only the eleven hostile
groups gain the defeated flag, HP0, and off-map X coordinate; their class,
identity, and Y coordinate remain intact.

All 47 normal aftermath frames were manually reviewed. The five hard frames
whose pixels differ from normal were compared side by side; the other 42 are
pixel-identical. Differences are limited to map/result animation and capture
phase. No frame has a broken name, class, commander, mercenary, portrait, map
sprite, or UI glyph.

The sequence preserves `레아드`, `제국군지휘관`, `엘윈`, `일반병`,
`헤인`, `리아나`, `쉐리`, `아론`, `레스터`, and `스코트`, including
`성스러운 지팡이 획득!`. The result roster visibly contains `아군`,
`엘윈`, `헤인`, `쉐리`, `아론`, `키스`, `레스터`, `스코트`,
`리아나`, and `라나`, plus `POINT 3400P`. The save menu is intact in both
profiles.

Machine-readable evidence is generated by
`tools/verify_scenario23_current_result_surface.py` in
`localization/scenario23_current_result_surface_regression.json`.

These GSTs are isolated verification fixtures. Player progress still migrates
by loading the `.srm` through the in-game menu under the current ROM and then
creating a fresh savestate.
