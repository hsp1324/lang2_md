# Scenario 25 current result-surface verification

Scenario 25 has fresh preparation-to-result evidence on both current candidates:

| Profile | Candidate | Runtime-clear probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CB53` / `00f0dec…` | `6472` / `8bf91cb…` | `normal/runtime01` |
| Hard | `E15E` / `f3d5e05…` | `7A7D` / `e705f51…` | `hard/runtime01` |

Neither candidate was changed or promoted. Each ignored diagnostic ROM keeps
all nine player deployments, all twelve fixed records, identities, classes,
levels, positions, combat values, and event handlers byte-identical to its
input candidate. Opening Start preserves player groups 0..8 and event-spawned
allied Jessica group 9, then marks only hostile groups 10..20 defeated.

The probe changes 202 bytes including its checksum. Replaying the exact normal
diagnostic delta over the current hard candidate has zero hard-mode conflicts;
the only subsequent operation is checksum recalculation.

## Fresh normal and hard flow

Both profiles were entered afresh through the selector and identified Scenario
25. Eleven of twelve source fixed records match because source record 0 is
correctly replaced at runtime by allied Jessica (`소서러`, HP10 at 16,28).
Retained RAM snapshots prove her complete group record and player groups 0..8
are byte-identical before and after Start. Both runs passed preparation,
automatic deployment, the turn-1 command surface, the Korean Start menu,
ordinary End Turn, all 42 completion frames, `전과보고`, and the real `저장`
screen.

All 42 normal aftermath frames and all 26 hard frames whose pixels differ were
manually reviewed. Differences in frames 1..24 and the command/Start surfaces
are confined to one 64x32 top-map unit animation box. Frame 29 differs only by
dialogue typewriter progress, and frame 42 only by result-unit animation. No
frame has a broken name, class, commander, mercenary, portrait, map sprite, or
UI glyph.

The sequence preserves `레온`, `제국병`, `제국군지휘관`, `레아드`,
`엘윈`, `헤인`, `쉐리`, `라나`, and `리아나`. The result roster visibly
contains `아군`, `엘윈`, `헤인`, `쉐리`, `아론`, `키스`, `레스터`,
`스코트`, `리아나`, and `라나`, plus `POINT 5200P`. The save menu is intact
in both profiles.

Machine-readable evidence is generated by
`tools/verify_scenario25_current_result_surface.py` in
`localization/scenario25_current_result_surface_regression.json`.

These GSTs are isolated verification fixtures. Player progress still migrates
by loading the `.srm` through the in-game menu under the current ROM and then
creating a fresh savestate.
