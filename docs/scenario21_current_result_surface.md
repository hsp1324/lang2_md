# Scenario 21 current result-surface verification

Scenario 21 now has fresh preparation-to-result evidence on both current
candidates:

| Profile | Candidate | Runtime-clear probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CB53` / `00f0dec…` | `F4DA` / `7b16954…` | `normal/runtime01` |
| Hard | `E15E` / `f3d5e05…` | `0AE5` / `eb41d39…` | `hard/runtime01` |

Neither candidate was changed or promoted. Each ignored diagnostic ROM keeps
all eight player deployments, all eleven fixed records, identities, classes,
levels, positions, combat values, and event handlers byte-identical to its
input candidate. The current normal records additionally match the Japanese
source; the hard candidate intentionally retains its own balance data. Opening
Start marks only hostile runtime groups 8..10 and 12..18 defeated. It does not
touch the rescued Lana in group 11.

The probe changes 185 bytes including its checksum. Replaying the exact normal
diagnostic delta over the current hard candidate has zero hard-mode conflicts;
the only subsequent operation is checksum recalculation.

## Fresh normal and hard flow

Both profiles were entered afresh through the selector and identified Scenario
21 with an exact 11/11 fixed-record match. They then passed preparation,
automatic deployment, the turn-1 command surface, the Korean Start menu, the
ordinary End Turn command, all completion events, every level-up/class-change
page, `전과보고`, and the real `저장` screen.

The retained Start-menu GSTs prove every declared hostile group is defeated,
while Lana remains class `60`, name ID `0C`, HP10, undefeated, at `(37,11)`.
This is a runtime-only entry into the untouched completion handlers, not a
production-data or balance change.

All 36 normal aftermath frames were manually reviewed. Hard frames 1..35 are
pixel-identical to normal; the final result frame differs only because the
lower unit animation was sampled at another phase. The dialogue surfaces keep
`리빙아머`, `서큐버스`, `리치`, `크라켄`, `제국군지휘관`, every allied
speaker name, and the surrounding status UI intact. Sherry's class-change page
also preserves `로드`, `호크나이트`, and `클래스체인지 가능`.

The result roster visibly contains the faction heading `아군`, followed by
`엘윈`, `헤인`, `쉐리`, `아론`, `키스`, `레스터`, `제시카`, and
`스코트`, plus `POINT 31100P`. The first left-column row is `아군`; it is not
a duplicated `아론` commander. The save menu is intact in both profiles.

Machine-readable evidence is generated by
`tools/verify_scenario21_current_result_surface.py` in
`localization/scenario21_current_result_surface_regression.json`.

## Rejected and isolated attempts

- Loading the historical `s21_completion_8a18_fresh` GST under the current ROM
  black-wiped and crashed, so it is rejected as a cross-build continuation.
- A Japanese runtime-clear ROM reached preparation, but the Korean-specific
  cursor detector did not identify that Japanese surface. No Japanese result
  claim is made from that attempt.
- A zoomed result review resolved the apparent first-row `아론` duplication as
  the correct `아군` faction heading.

These GSTs are isolated verification fixtures. Player progress still migrates
by loading the `.srm` through the in-game menu under the current ROM and then
creating a fresh savestate.
