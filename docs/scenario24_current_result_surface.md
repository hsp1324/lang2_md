# Scenario 24 current result-surface verification

Scenario 24 has fresh preparation-to-result evidence on both current candidates:

| Profile | Candidate | Runtime-clear probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CB53` / `00f0dec…` | `5812` / `2fddd78…` | `normal/runtime02` |
| Hard | `E15E` / `f3d5e05…` | `6E1D` / `2c64319…` | `hard/runtime02` |

Neither candidate was changed or promoted. Each ignored diagnostic ROM keeps
all nine player deployments, all eleven fixed records, identities, classes,
levels, positions, combat values, and event handlers byte-identical to its
input candidate. Opening Start marks only hostile runtime groups 9..19 defeated
and leaves player groups 0..8 byte-identical in retained RAM snapshots.

The probe changes 202 bytes including its checksum. Replaying the exact normal
diagnostic delta over the current hard candidate has zero hard-mode conflicts;
the only subsequent operation is checksum recalculation. The rejected normal
`runtime01` stopped after valid preparation because the shared runner expected
the Scenario 23 name for the final hostile-group constant. It made no gameplay
claim and was superseded by `runtime02` after the builder interface was aligned.

## Fresh normal and hard flow

Both profiles were entered afresh through the selector and identified Scenario
24 with all eleven fixed records matched. Both runs passed preparation,
automatic deployment, the turn-1 command surface, the Korean Start menu,
ordinary End Turn, all 42 completion frames, `전과보고`, and the real `저장`
screen.

All 42 normal aftermath frames were manually reviewed. Frames 1..41 are
pixel-identical in the hard run. Frame 42 differs by only 13 pixels inside a
7x8 lower-unit animation box on the result screen; all text and name areas are
identical. No frame has a broken name, class, commander, mercenary, portrait,
map sprite, or UI glyph.

The sequence preserves `데몬로드`, `케르베로스`, `리치`, and
`뱀파이어로드`. The result roster visibly contains `아군`, `엘윈`, `헤인`,
`쉐리`, `아론`, `키스`, `레스터`, `스코트`, `리아나`, and `라나`, plus
`POINT 55900P`. The save menu is intact in both profiles.

Machine-readable evidence is generated by
`tools/verify_scenario24_current_result_surface.py` in
`localization/scenario24_current_result_surface_regression.json`.

These GSTs are isolated verification fixtures. Player progress still migrates
by loading the `.srm` through the in-game menu under the current ROM and then
creating a fresh savestate.
