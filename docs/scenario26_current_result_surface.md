# Scenario 26 current result-surface verification

Scenario 26 has fresh preparation-to-result evidence on both focused current
candidates containing the corrected `보젤과` particle:

| Profile | Candidate | Runtime-clear probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CAF3` / `178e704…` | `031A` / `c038e76…` | `normal/runtime02` |
| Hard | `E0FE` / `9c6282c…` | `1925` / `2d0cae9…` | `hard/runtime02` |

Neither candidate was released or promoted. Each ignored diagnostic ROM keeps
all ten player deployments, all ten fixed enemy records, identities, classes,
levels, positions, combat values, and event handlers byte-identical to its
input candidate. Opening Start preserves player groups 0..9 and marks only
hostile groups 10..19 defeated.

The probe changes 185 bytes including its checksum. Replaying the exact normal
diagnostic delta over the focused hard candidate has zero hard-mode conflicts;
the only subsequent operation is checksum recalculation.

## Fresh normal and hard flow

Both profiles were entered afresh through the selector and identified Scenario
26 with all ten fixed records matching. The normal selector's first automation
attempt timed out after 80 confirmations; its automatic clean retry entered the
scenario on attempt 2. The hard profile entered on attempt 1. Only those
successful attempts carry gameplay evidence.

Retained RAM snapshots prove player groups 0..9 are byte-identical before and
after Start. Both accepted runs passed preparation, automatic deployment, the
turn-1 command surface, the Korean Start menu, ordinary End Turn, all 42
completion and level-up frames, `전과보고`, and the real `저장` screen.

All 42 normal aftermath frames and the three differing hard frames were
manually reviewed. Frames 32 and 34 differ only in small opposite-edge map
animation boxes. Frame 42 differs only in result-unit animation. Preparation,
the command surface, Start menu, and save menu are pixel-identical. No frame
has a broken name, class, commander, mercenary, portrait, map sprite, or UI
glyph.

The sequence preserves `에그베르트`, `제국군지휘관`, `제시카`, `레스터`,
`라나`, `리아나`, `엘윈`, `헤인`, and `쉐리`. The result roster visibly
contains `아군`, `엘윈`, `헤인`, `쉐리`, `아론`, `키스`, `레스터`,
`스코트`, `리아나`, `라나`, and `제시카`, plus `POINT 4240P`.

The old normal `runtime01` exposed the bad particle `보젤와` and is rejected.
The localized source record at `0x1B35B4` now uses `보젤과`; focused word
`7089` at `0x1B35C0` became `7029`. Both accepted live frame 21 captures render
`제시카: 에그베르트는 보젤과 싸울 강대한 힘이 급히 필요했죠…` cleanly.

Machine-readable evidence is generated by
`tools/verify_scenario26_current_result_surface.py` in
`localization/scenario26_current_result_surface_regression.json`.

These GSTs are isolated verification fixtures. Player progress still migrates
by loading the `.srm` through the in-game menu under the current ROM and then
creating a fresh savestate.
