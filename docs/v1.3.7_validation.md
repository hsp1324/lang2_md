# v1.3.7 Validation

Status: **reissued with the Scenario 1 dialogue follow-up**.

The immediately preceding v1.3.7 package completed a fresh-SRAM,
sequential-save route from Scenario 1 through all four secret scenarios,
Scenario 27, and the stable Fin screen. Original, Normal, and Hard passed all
31 route steps each (93 total). The 2026-08-12 reissue keeps that gameplay
implementation and changes only the reviewed Scenario 1 dialogue resources.
The exact ROM and BPS identities below supersede the earlier package.

## Locked candidate artifacts

| Profile | Translation | Balance | MD checksum | ROM SHA-256 |
| --- | --- | --- | --- | --- |
| Original | 1.3.7 | source | `4153` | `604d022080ae701a8b2ff0dd9f6906143e1483a74be5ac4ba9f8a2cffa051bff` |
| Normal | 1.3.7 | source | `8BF0` | `c10ef6a6487c7b6a49ce47cce1792e89521698818cc13ce7590c97794ced4580` |
| Hard | 1.3.7 | 1.3.7 | `2160` | `7d0c528c10d86367460792d06b95402d92557904ab62aea093ddda9d52081a4a` |

All three outputs are 4,194,304 bytes and retain SRAM descriptor
`5241F8200040000100403FFF` (`lang2-ko-sram-v1`). The Hard translation bump
includes the Hard-only loss-condition NPC survival compensation.

## Reproducible build and package checks

Run from the repository root with the verified Japanese ROM in
`roms/original/Langrisser II (Japan).md`:

```bash
python3 tools/build_v137_release_patches.py --rebuild-roms
python3 tools/build_v137_release_patches.py --check
python3 -m unittest tests.test_v137_release_integrity tests.test_v120_release_patcher
```

The BPS assets round-trip from Japanese source SHA-256
`a6e10e82b1e8fd32d8e4ae2ce76ab689cd789d93f854aa1788abc1e9795ddb3b`
to the three locked output hashes:

| Patch | Size | SHA-256 |
| --- | ---: | --- |
| `original-v1.3.7.bps` | 2,286,139 | `a12cb6434312e4ba330e0e2d89be3ae9f8f9892f87518b0a64a49d0eb5d1261a` |
| `normal-v1.3.7.bps` | 2,286,586 | `c17d2b86af34eccd019eea23a430982d08f04cde95ac0b714766ec48db90a658` |
| `hard-v1.3.7.bps` | 2,289,214 | `2c13a6ea841ce99825c2b1ab281a02d442f2333fe78cf0f69ebd1fd8518eaed3` |

Former delta descriptions belonged to invalidated candidates and are not a
valid size claim for the current source. The current implementation replaces the old
target-level refill wrapper with a one-shot fixed-raw-EXP wrapper, updates the
live-join visibility guard that arms it, and places the bounded Scenario 10
ownership helper at `0x31E380`. The visibility guard now treats the persisted
active scenario at `0xA49C` as authoritative during ordinary play and both
cold and warm title-screen LOAD. It reads the next-scenario scratch at
`0xA612` only when the scheduler stack proves that the outer callback is the
stock result scanner at `0x00CEC4`; a stale nonzero selector can no longer
override a loaded save.

The fixed raw grants are Keith `0x00`, Lester `0x90`, and Jessica `0x60`.
Each value reconstructs `(original tier-2 level - 1) * original class gauge`,
excluding the Japanese row's residual EXP bar.  The grant is identical across
Original, Normal, and Hard and across all three selected branches; the selected
class's own gauge determines the resulting LV/EXP, with no Hard-only addition.
Only the new one-shot `0xA5` live-join marker authorizes a grant.  A legacy
v1.3.6 `0x5A` marker is cleared without a grant, and a later Rune Stone restart
has no marker and therefore keeps the stock `LV1/EXP0` result.

The article-driven Hard correction covers exactly 19 side-03 NPCs whose death
or group annihilation is a loss condition in Scenarios 1, 2, 3, 4, 6, 7, 9,
11 and 18. Fixed non-playable NPCs receive the reviewed DF/D+ values directly.
Liana and Jessica pass through the stock playable-roster rewrite, so a Hard-only
post-processing hook adds the same scenario deltas to their live roster-derived
DF/D+ without changing class, level, AT, A+, placement or mercenaries. Xvfb
runtime entry checks matched all 19/19 targets; the five dynamic cases changed
only runtime offsets `0x3B` and `0x47` from the superseded candidate.

## Inherited sequential runtime acceptance

The completed route used isolated Xvfb displays, empty runtime homes and fresh
SRAM on the immediately preceding v1.3.7 hashes. Every step executed the real
victory result, experience/join/event processing and save menu; each produced
save fed the next route step. The reissue changes dialogue resources only; its
new exact hashes are covered by reproducible builds, BPS round trips and the
Scenario 1 reviewed-dialogue regression. Combat was shortened with
victory-near probe ROMs, so this is not a claim that 31 full battles were
manually played from their first turn.

### Scenario 12 first-turn RNG boundary

On the superseded pre-cold-load-fallback candidate, Original (`Pure`) and
Normal reached identical runtime groups; the observed WRAM difference was one
VBlank at the LCG seed boundary. Original reproduced both the natural `HP0`
defeat endpoint and turn 2 with `HP1`; Normal and Hard also reached the natural
`HP0` endpoint. Those captures belong to invalidated ROM hashes and are not
final acceptance evidence for the current candidate.

Accordingly, the inspection policy accepts either of the two normal Scenario 12
endpoints—turn 2 or a natural `HP0` defeat—when it is backed by the required
artifact hashes for each of Original, Normal and Hard. This is an RNG-boundary
acceptance rule, not a waiver of the three-profile check. The predecessor's
exact-ROM sequential route completed; the broader post-release audit remains
tracked separately from this dialogue-only reissue.

- Recheck Original, Normal and Hard Scenario 1-through-ending sequential play,
  including save/result transitions and alternate scenarios, in the broader
  post-release audit.
- Natural joins for Keith (Scenario 7), Lester (Scenario 10) and Jessica
  (Scenario 11), with Lester's default and Croco Lord selection paths.
- Legacy Fighter `LV10`, `LV11` and `LV12` migration for Keith and Lester.
- Rune Stone restart for Keith, Lester and Jessica from tiers 2, 3, 4 and 5.
- Keith Hawk Lord and Lester Croco Lord map/status/stat/EXP data plus actual
  side-view mounted attack animation, with no Sister, Dragon Knight, Vampire or
  Serpent Knight fallback.
- Scenario 6 well Rune Stone accessibility, fixed enemy/NPC identity fields,
  late-game enemies, Scenario 31, the X4-to-Scenario-27 transition and ending.

The listed items remain part of the broader post-release audit. The dialogue
reissue does not change their gameplay implementation or save-data contract.
