# Scenario 17 current result-surface verification

Scenario 17 now has fresh preparation-to-result evidence on both current
candidates:

| Profile | Candidate | Diagnostic probe | Accepted run |
| --- | --- | --- | --- |
| Normal | `CB53` / `00f0dec…` | `6642` / `9f116ad…` | `normal/result10` |
| Hard | `E15E` / `f3d5e05…` | `250C` / `0d5aa7d…` | `hard/result02` |

The candidates themselves were not changed or promoted. The ignored probe
keeps every Scenario 17 commander identity, class, level, coordinate, hidden
record, and event handler. It removes enemy mercenaries, zeros their fixed
AT/DF bonuses, and moves only Elwin next to the source Bernhardt position.
The hard probe is the exact 129-byte normal diagnostic delta over the hard
candidate followed by checksum recalculation; 44 bytes overlap hard-mode
enemy-stat changes and are reported explicitly.

## Stock-HP two-attack path

The first current probe attack defeated HP10 Bernhardt immediately and entered
Scenario 17's early-ending branch. Raising him to HP19 was rejected because it
exceeded the engine's HP10 range and froze the battle animation. The accepted
diagnostic wrapper changes only Elwin's runtime AT:

1. Before Elwin acts, opening Start changes AT23 to AT5. Bernhardt remains
   HP10.
2. The first ordinary Attack leaves Bernhardt alive and marks Elwin acted.
3. After all post-battle dialogue has closed, opening Start restores Elwin to
   AT23. The ordinary End Turn command is used.
4. Turn 2 resets Elwin's acted flag. Bernhardt's stock turn-start recovery is
   preserved within HP1..10.
5. The second ordinary Attack enters the normal victory aftermath. The runner
   can repeat later turns if a future hard-mode balance changes leave the boss
   alive.

The retained GST sequence is:

| State | Normal | Hard |
| --- | --- | --- |
| First Start | Elwin acted 0, AT5; Bernhardt HP10 | same |
| First attack settled | Elwin acted 1, AT5; Bernhardt HP4 | Elwin acted 1, AT5; Bernhardt HP6 |
| Post-action Start | Elwin acted 1, AT23; Bernhardt unchanged | same invariant |
| Turn 2 command | Elwin acted 0, AT23; Bernhardt HP7 | Elwin acted 0, AT23; Bernhardt HP9 |

This is renderer and flow evidence, not battle-balance evidence and not a
distributable savestate.

## Reviewed surfaces

Both profiles freshly enter Scenario 17 with an exact 11/11 fixed-record
identity match, pass preparation, automatic deployment, the turn-1 command
surface, post-battle dialogue, turn 2, the victory aftermath, `전과보고`, and
`저장`.

The result roster was manually reviewed in both captures. `키스`, `레스터`,
`제시카`, and `스코트` are intact, as are `전과보고` and `POINT 4200P`.
There are no broken dynamic Hangul tiles or damaged commander sprites. The
save menu shows `저장`, `1 시나리오 27`, and `다음 시나리오` without damage.

Machine-readable evidence is generated by
`tools/verify_scenario17_current_result_surface.py` in
`localization/scenario17_current_result_surface_regression.json`.

## Rejected attempts

Ten rejected attempts remain recorded in the JSON report. They include the
early-ending HP1 route, a lost Attack input, unsafe offline GST reloads, an
omitted Up target input, Start during pre-battle dialogue, the one-hit AT23
ending, the invalid HP19 freeze, a transient post-battle map misclassification,
an over-strict rejection of stock regeneration, and the original fixed
two-attack hard-run assumption. None is counted as accepted evidence.

Player save migration remains unchanged: load the `.srm` through the in-game
menu under the current ROM, then create a fresh savestate. The isolated GSTs in
this verification are not user migration artifacts.
